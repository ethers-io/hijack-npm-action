{
  "name": "hijack-npm-action",
  "version": "0.0.1",
  "description": "Faux NPM registry to hijack install requests.",
  "main": "index.js",
  "private": true,
  "pblicConfig": {
    "access": "private"
  },
  "scripts": {
    "auto-build": "npm run build -- -w",
    "build": "tsc --project tsconfig.json",
    "start": "node index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "type": "module",
  "keywords": [
    "npm",
    "registry",
    "hijack"
  ],
  "author": "Richard Moore <me@ricmoo.com>",
  "license": "MIT",
  "devDependencies": {
    "typescript": "^5.0.4"
  },
  "dependencies": {
    "@types/node": "^18.15.13"
  }
}
